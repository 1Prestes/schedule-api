name: "AWS"
description: ""
inputs:
  aws-access-key-id:
    description: 'aws-access-key-id'
    required: true
  aws-secret-access-key:
    description: 'aws-secret-access-key'
    required: true
  aws-region:
    description: 'aws-region'
    required: true
    default: 'us-east-1'
runs:
  using: "composite"
  steps:
    - name: Console Global Secret
      run: echo ${{secrets.SECRET_TEST}}
    - name: Console Secret2
      run: echo ${{secrets.SECRET_TEST2}}

    - name: AWS - Configure credentials
      id: aws-credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
          aws-access-key-id: 'testekeyid'
          aws-secret-access-key: 'testeaccesskey'
          aws-region: ${{ inputs.aws-region }}