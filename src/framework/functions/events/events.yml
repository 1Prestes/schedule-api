createEvent:
  handler: ${self:custom.funcDir}/events/createEvent.handler
  events:
    - http:
        path: ${self:custom.baseApi}/events
        method: post
        cors:
          origin: '*'
          headers: '*'
listEvents:
  handler: ${self:custom.funcDir}/events/listEvents.handler
  events:
    - http:
        path: ${self:custom.baseApi}/user/{id}/events
        method: get
        cors:
          origin: '*'
          headers: '*'
